{
    "$schema": "https://turborepo.com/schema.json",
    "tasks": {
        "watch": {
            "persistent": true,
            "outputs": [ "dist/**" ],
            "inputs": [ "src/**" ],
            "cache": false
        }
    }
}